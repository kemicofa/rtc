[package]
name = "gcp-traces"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.99"
reqwest = "0.12.23"
serde = { version = "1.0", features = ["derive"] }
gcp_auth = "0.12"            # loads ADC and fetches access tokens
serde_json = "1"
